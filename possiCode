public class Main {
    public static void main(String[] args) {
        // Fetch stock quote data
        JSONObject stockQuote = stockAPI.getStockQuote("AAPL");
        
        // Extract relevant information from the JSON response
        String symbol = (String) stockQuote.get("symbol");
        String name = (String) stockQuote.get("name");
        double price = Double.parseDouble(stockQuote.get("price").toString());
        double change = Double.parseDouble(stockQuote.get("change").toString());
        double percentChange = Double.parseDouble(stockQuote.get("percentChange").toString());

        // Create an instance of the data class and populate it with the extracted information
        data stockData = new data(symbol, name, price, change, percentChange);

        // Now you can use the stockData object to access and manipulate the stock-related information
        System.out.println("Symbol: " + stockData.getSymbol());
        System.out.println("Name: " + stockData.getName());
        System.out.println("Price: " + stockData.getPrice());
        System.out.println("Change: " + stockData.getChange());
        System.out.println("Percent Change: " + stockData.getPercentChange());
    }
}


basically try to have it so that similar to the weatherApp where you search for something it displays it but displays all of the 
metadata and you can then add your stock to a portfolio that you can track all of your stocks in